# Copy this file to signing.properties. Gradle loads signing.properties automatically.
# You can also provide the same keys via ~/.gradle/gradle.properties, -P... or environment variables.
# Never commit signing.properties, keystores, passwords, or API keys.

# Release signing keystore.
# Local release builds can run without these, but real APK/AAB releases should be signed.
LOQIN_RELEASE_STORE_FILE=keys/loqin-release.jks
LOQIN_RELEASE_STORE_PASSWORD=change-me
LOQIN_RELEASE_KEY_ALIAS=loqin
LOQIN_RELEASE_KEY_PASSWORD=change-me

# Google Maps key for the location picker.
# Keep this out of committed gradle.properties/local.properties.
MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# Release signing is only wired up when all four LOQIN_RELEASE_* signing values
# above are non-blank. Without them, release builds produce unsigned APK/AAB output.
