<?xml version="1.0" encoding="utf-8"?>
<com.oliver.loqin.ui.SquareCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rowRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/foqos_surface"
    app:cardCornerRadius="18dp"
    app:cardElevation="1dp"
    app:cardUseCompatPadding="false"
    app:strokeColor="@color/foqos_outline_variant"
    app:strokeWidth="1dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="6dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="10dp"
            android:gravity="center_horizontal"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/ivAppIcon"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:contentDescription="@string/app_icon"
                android:src="@android:drawable/sym_def_app_icon" />

            <TextView
                android:id="@+id/tvLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:gravity="center"
                android:textColor="?android:attr/textColorPrimary"
                android:textSize="12sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvSub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:textColor="?android:attr/textColorSecondary"
                android:textSize="10sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:visibility="gone" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/overlayActions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:orientation="horizontal">

            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">

                <ImageButton
                    android:id="@+id/btnLimit"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/set_daily_limit"
                    android:padding="4dp"
                    app:srcCompat="@drawable/schedule_24"
                    app:tint="?attr/colorOnSurface" />

                <View
                    android:id="@+id/viewLimitDot"
                    android:layout_width="8dp"
                    android:layout_height="8dp"
                    android:layout_gravity="top|end"
                    android:visibility="gone"
                    android:background="@drawable/limit_dot" />
            </FrameLayout>
        </LinearLayout>

        <LinearLayout
            android:id="@+id/overlayRules"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btnWebsiteRules"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/app_picker_website_rules_open"
                android:visibility="gone"
                android:padding="4dp"
                app:srcCompat="@drawable/language_24"
                app:tint="?attr/colorOnSurface" />

            <ImageButton
                android:id="@+id/btnInAppRules"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/app_picker_in_app_rules_open"
                android:visibility="gone"
                android:padding="4dp"
                app:srcCompat="@drawable/layers_24"
                app:tint="?attr/colorOnSurface" />
        </LinearLayout>

        <ImageView
            android:id="@+id/ivChecked"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="top|end"
            android:visibility="gone"
            android:contentDescription="@null"
            android:src="@drawable/check_circle_24"
            app:tint="?attr/colorPrimary" />
    </FrameLayout>
</com.oliver.loqin.ui.SquareCardView>
