<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Pages -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false" />

    <!-- Fallback host used only when the OS reports API 34+ but is missing finalized API-34 framework members. 
         It avoids ViewPager2's AndroidX accessibility action setup. -->
    <FrameLayout
        android:id="@+id/compatPageContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:visibility="gone" />

    <!-- Top Header Bar -->
    <LinearLayout
        android:id="@+id/onboardingHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingTop="8dp"
        android:paddingEnd="12dp"
        android:paddingBottom="4dp">

        <ImageButton
            android:id="@+id/btnHeaderBack"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/keyboard_arrow_left_24"
            android:contentDescription="@string/onb_back"
            android:visibility="invisible"
            app:tint="@color/foqos_on_surface" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnHeaderSkip"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/onb_skip"
            android:textColor="@color/foqos_on_surface_variant"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textAllCaps="false" />
    </LinearLayout>

    <!-- Bottom action bar -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:layout_gravity="bottom"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardCornerRadius="0dp"
        app:cardElevation="0dp"
        app:cardUseCompatPadding="false"
        app:strokeWidth="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="0dp"
            android:paddingTop="4dp"
            android:paddingEnd="0dp"
            android:paddingBottom="4dp">

            <LinearLayout
                android:id="@+id/pageIndicator"
                android:layout_width="match_parent"
                android:layout_height="18dp"
                android:layout_marginBottom="12dp"
                android:orientation="horizontal"
                android:gravity="center" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_optional_setup"
                style="@style/Switchly.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="52dp"
                android:layout_marginBottom="8dp"
                android:visibility="gone"
                android:text="@string/onb_optional_setup_action"
                android:textSize="15sp"
                android:textAllCaps="false"
                app:icon="@drawable/keyboard_arrow_right_24"
                app:iconGravity="textEnd"
                app:iconPadding="8dp" />

            <!-- Retained hidden for backwards code compatibility -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_skip"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone" />

            <View
                android:id="@+id/onboardingButtonSpacer"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone" />

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="56dp">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_next"
                    style="@style/Switchly.Button"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:cornerRadius="28dp"
                    android:text="@string/onb_continue"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textAllCaps="false" />

                <!-- Overlay arrow: does NOT affect button text centering -->
                <ImageView
                    android:id="@+id/nextArrow"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_marginEnd="20dp"
                    android:layout_gravity="end|center_vertical"
                    android:clickable="false"
                    android:focusable="false"
                    android:alpha="0.9"
                    android:contentDescription="@null"
                    android:src="@drawable/keyboard_arrow_right_24"
                    app:tint="@android:color/white" />
            </FrameLayout>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
